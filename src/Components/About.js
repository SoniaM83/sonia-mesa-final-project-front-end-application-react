import React from 'react';

const About = () => {
    return (
        <div id="about">
            <h2>About</h2>
            <p>Use this app to create and review flashcards. Navigate through cards using the buttons, and add your own using the form below.</p>
        </div>
    );
};

export default About;